# Scratchpad — Plan d'implémentation Widget ThirdPartyApps — 2026-07-19

## Status: COMPLETED

## What was done

### Backend (Program.cs)
1. ✅ Added `GetTriggeringAppName()` — returns first monitored app process name, or "NONE". Uses `Volatile.Read` for thread-safe access to `thirdPartyApps`.
2. ✅ Added `RemoveThirdPartyApp(string name)` — validates (protected app check), removes from runtime variable + INI, calls `ReloadAndApplyConfig`, returns status codes. Uses `Volatile.Read`/`Volatile.Write` for thread safety.
3. ✅ Added `TRIGGER` and `APPS` cases to `ProcessCommand` switch.
4. ✅ Added `REMOVEAPP` handling in `StartHttpServer` with query string `name` parameter extraction.
5. ✅ Build passes: 0 errors, 0 warnings.

### Widget (RetroBatGameModeWidget)
6. ✅ Created `AppsListPage.xaml` — list view with Back, Refresh buttons, ItemsControl with Remove per item, empty state.
7. ✅ Created `AppsListPage.xaml.cs` — navigation parameter, async loading/parsing/rendering, remove handler with refresh, back navigation.
8. ✅ Added `TriggerText` to `WidgetPage.xaml` inside status border.
9. ✅ Added `AppsButton` to `WidgetPage.xaml`.
10. ✅ Added `RefreshTriggerApp()` to `WidgetPage.xaml.cs`.
11. ✅ Added `AppsButton_Click` handler (Frame.Navigate to AppsListPage).
12. ✅ Updated `SetButtonsEnabled` to include `AppsButton`.
13. ✅ Registered `AppsListPage.xaml+.cs` in `.csproj` (Compile + Page items).

### Key design decisions applied from plan
- Thread safety: `Volatile.Read`/`Volatile.Write` for `thirdPartyApps` in HTTP thread vs monitoring loop.
- `ReloadAndApplyConfig` called synchronously from HTTP thread (existing pattern from Target mode, line 3548).
- `Uri.EscapeDataString` for app names with special characters.
- `FetchAsync` duplicated between pages (plan V1 approach, refactor later).
- Timer stays alive on WidgetPage after Navigate — expected behavior per plan.

### Build status
- Backend: `dotnet build` → 0 errors, 0 warnings
- Widget : Pour le build UWP (WAP), utiliser Visual Studio ou MSBuild avec le `.wapproj`